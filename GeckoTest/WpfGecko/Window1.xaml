<Window x:Class="WpfGecko.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfGecko"
        mc:Ignorable="d"
        Title="" 
        Height="450" Width="800" 
        AllowsTransparency="True" 
        Background="Transparent" 
        WindowStyle="None" 
        ShowInTaskbar="False"
        Topmost="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseDoubleClick="Window_MouseDoubleClick"
        ResizeMode="NoResize"
        Closed="Window_Closed"
        ContentRendered="Window_ContentRendered"
        >
    <Grid>
        <Image x:Name="bgImg" Stretch="Fill">
            <Image.ContextMenu>
                <ContextMenu>
                    <MenuItem InputGestureText="DoubleClick" Click="MenuItem_Click_1"/>
                    <MenuItem InputGestureText="Exit" Click="MenuItem_Click"/>
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
    </Grid>
</Window>
